botesito:
  port: 8080
  spec_path: "docs/openapi.json"
  api_token: "dev-token"

# Leave the telegram section out entirely until you have real values:
# empty strings are rejected by the config validator and abort the load.
#
# telegram:
#   bot_token: "123456:ABC-DEF..."
#   chat_id: 123456789
